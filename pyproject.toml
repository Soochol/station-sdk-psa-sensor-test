[project]
name = "station-sdk-psa-sensor-test"
version = "0.1.0"
description = "PSA sensor test firmware for STM32H723"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "station-service-sdk>=1.0.0",
]
